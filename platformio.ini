[platformio]
default_envs = lilygo-t-display-s3

[env]
; Pinned: Arduino-ESP32 core 3.x (espressif32 platforms newer than 6.x) renames
; the WiFi client classes and ships its own NetworkManager — see issue #5.
platform = espressif32@6.10.0
framework = arduino

; Serial Monitor Configuration
monitor_speed = 115200
monitor_filters =
    default
    time

; Upload Configuration
upload_speed = 921600

; Build Configuration
build_flags =
    -DCORE_DEBUG_LEVEL=3
    -DBOARD_HAS_PSRAM
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DLV_CONF_INCLUDE_SIMPLE
    -DMQTT_MAX_PACKET_SIZE=1024
    -I include

; Library Dependencies
lib_deps =
    lvgl/lvgl@~8.3.11
    knolleary/PubSubClient@^2.8
    bblanchon/ArduinoJson@^7.0.4

; Extra build settings
board_build.flash_mode = qio
board_build.f_flash = 80000000L
board_build.f_cpu = 240000000L

; Filesystem settings
board_build.filesystem = littlefs

[env:lilygo-t-display-s3]
board = lilygo-t-display-s3

build_flags =
    ${env.build_flags}
    -DTESLA_BMS_TARGET_LILYGO_T_DISPLAY_S3
    -DUSE_MQTT=1

; Extra build settings
board_build.partitions = default.csv

[env:esp32-s3-lcd-1_3-b]
board = esp32-s3-lcd-1_3-b
upload_speed = 921600

build_unflags =
    -DCORE_DEBUG_LEVEL=3
    -DARDUINO_USB_CDC_ON_BOOT=1

build_flags =
    ${env.build_flags}
    -DCORE_DEBUG_LEVEL=0
    -DARDUINO_USB_CDC_ON_BOOT=0
    -DTESLA_BMS_TARGET_ESP32_S3_LCD_1_3_B
    -DUSE_MQTT=1

board_build.partitions = huge_app.csv
